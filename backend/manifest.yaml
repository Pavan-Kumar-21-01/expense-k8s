apiVersion: v1
kind: ConfigMap
metadata:
  name: backend
  namespace: expense
data:
  DB_HOST: mysql-svc
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: backend
  namespace: expense
  labels:
    app: backend
    tier: app
    project: expense
spec:
  replicas: 3
  selector:
    matchLabels:
      app: backend
      tier: app
      project: expense
  template:
    metadata:
      labels:
        app: backend
        tier: app
        project: expense
    spec:
      containers:
      - name: backend-cont
        image: 945360925177.dkr.ecr.us-east-1.amazonaws.com/expense/backend
        envFrom:
        - configMapRef:
            name: backend
---
apiVersion: v1
kind: Service
metadata:
  namespace: expense
  name: backend-svc
spec:
  selector:
    app: backend
    tier: app
    project: expense
  ports:
  - name: backend-port
    port: 8080
    targetPort: 8080

